import 'package:flutter/material.dart';

class DesktopDesigner extends StatelessWidget {
  const DesktopDesigner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Desktop');
  }
}